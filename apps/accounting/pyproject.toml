[project]
name = "production-rag-accounting"
version = "0.1.0"
description = "Accounting niche for production-rag — US public-company filings (SEC EDGAR)"
requires-python = ">=3.12"
dependencies = [
  "production-rag-core",
  "fastapi>=0.115.0",
  "uvicorn[standard]>=0.34.0",
]

[tool.uv.sources]
production-rag-core = { path = "../../packages/core" }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/accounting"]
